{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --max-requests 1000 app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}